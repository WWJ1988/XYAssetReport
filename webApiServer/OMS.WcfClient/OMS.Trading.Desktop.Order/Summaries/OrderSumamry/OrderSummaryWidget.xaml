<UserControl x:Class="OMS.Trading.Desktop.Order.Summaries.OrderSumamry.OrderSummaryWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:framework="clr-namespace:OMS.Framework.Desktop.Common.Controls;assembly=OMS.Framework.Desktop.Common"
             mc:Ignorable="d">
    <StackPanel>
        <StackPanel Background="Black" Height="25">
            <TextBlock Foreground="White" Text="{Binding Title}" TextAlignment="Center" Padding="2" FontSize="15" FontWeight="Bold"/>
        </StackPanel>
        <DataGrid IsReadOnly="True" AutoGenerateColumns="False" ItemsSource="{Binding PLSummaryData}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Long P/L" Binding="{Binding LongPL}"/>
                <DataGridTextColumn Header="Short P/L" Binding="{Binding ShortPL}" />
                <DataGridTextColumn Header="Total P/L" Binding="{Binding TotalPL}" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
